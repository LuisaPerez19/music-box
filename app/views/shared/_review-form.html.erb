<%= form_with(model: [instrument, Review.new]) do |form| %>
  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>
  <div class="actions">
    <%= form.submit "Submit Review" %>
  </div>
<% end %>
